<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd" >
<mapper namespace="com.hh.repository.RiskPreventRepository">
	<!-- RISK_PREVENTIONテーブルとバインドする -->
	<select id="getPreventYear" resultType="RiskPreventionList">
		SELECT YEAR(OPEN_DATE) AS OPEN_DATE FROM risk_prevention ORDER BY OPEN_DATE ASC
	</select>
	<select id="getPreventMonth" resultType="RiskPreventionList">
		SELECT MONTH(OPEN_DATE) AS OPEN_DATE FROM risk_prevention ORDER BY OPEN_DATE ASC
	</select>	
</mapper>